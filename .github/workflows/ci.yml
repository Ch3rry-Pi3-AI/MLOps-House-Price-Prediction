name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.13"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Upgrade pip and install uv
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade uv

      - name: Cache uv packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: uv-${{ runner.os }}-${{ matrix.python-version }}-${{ hashFiles('uv.lock', 'requirements.txt') }}
          restore-keys: |
            uv-${{ runner.os }}-${{ matrix.python-version }}-

      - name: Create venv with uv & install deps
        run: |
          uv venv --python python${{ matrix.python-version }}
          source .venv/bin/activate
          if [ -f requirements.txt ]; then
            uv pip install -r requirements.txt
          fi
          # Ensure pytest present even if not listed
          uv pip install pytest

      - name: Run tests
        run: |
          source .venv/bin/activate
          pytest -q

  data-processing:
    name: Data processing & features (invoke)
    needs: tests
    runs-on: ubuntu-latest
    # Only run if the raw CSV exists in the repo (avoids failures on branches without data fixtures)
    if: ${{ hashFiles('data/raw/house_data.csv') != '' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Upgrade pip and install uv
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade uv

      - name: Cache uv packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: uv-${{ runner.os }}-3.13-${{ hashFiles('uv.lock', 'requirements.txt') }}
          restore-keys: |
            uv-${{ runner.os }}-3.13-

      - name: Create venv with uv & install deps (incl. invoke)
        run: |
          uv venv --python python3.13
          source .venv/bin/activate
          if [ -f requirements.txt ]; then
            uv pip install -r requirements.txt
          fi
          # Ensure invoke is available even if not in requirements
          uv pip install invoke

      - name: Show repo tree (debug)
        run: |
          pwd
          ls -la
          echo "--- src candidates ---"
          find src -maxdepth 3 -type f -name "*.py" -print || true

      - name: Ensure directories
        run: |
          source .venv/bin/activate
          inv ensure-dirs

      - name: Preprocess (invoke)
        run: |
          source .venv/bin/activate
          inv preprocess \
            --input data/raw/house_data.csv \
            --output data/processed/cleaned_house_data.csv \
            --policy filter \
            --target price \
            --iqr 1.5 \
            --index false

      - name: Engineer features (invoke)
        run: |
          source .venv/bin/activate
          inv engineer \
            --input data/processed/cleaned_house_data.csv \
            --output data/processed/engineered_features.csv \
            --preprocessor models/trained/preprocessor.pkl

      - name: Upload engineered data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: engineered-features
          path: data/processed/engineered_features.csv

      - name: Upload preprocessor
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: preprocessor
          path: models/trained/preprocessor.pkl
